Each environment should provide it's own `WorkspaceConfig`. (see core/models.py)

It apply action and return observations (A list of `RobotObservation`)

The code to grab observation, as well as apply action, should be put inside the env's folder

